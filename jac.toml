[plugins.byllm.model]
# Featherless.AI is the recommended path — see audit.jac `_pick_model()`
# which routes to Featherless when FEATHERLESS_API_KEY is set. This
# default is only used if the runtime picker is bypassed.
default_model = "openai/Qwen/Qwen2.5-14B-Instruct"

[dependencies.npm]
react = "^18.2.0"
react-dom = "^18.2.0"
react-router-dom = "^6.22.0"
react-error-boundary = "^5.0.0"
react-hook-form = "^7.71.0"
zod = "^4.3.6"
"@hookform/resolvers" = "^5.2.2"

[dependencies.npm.dev]
vite = "^6.4.1"
"@vitejs/plugin-react" = "^4.2.1"
typescript = "^5.3.3"
"@types/react" = "^18.2.0"
"@types/react-dom" = "^18.2.0"
